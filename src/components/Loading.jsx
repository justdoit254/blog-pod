const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen text-white text-xl bg-amber-200">
      <span className="animate-pulse">Loading ...</span>
    </div>
  );
};

export default Loading;
